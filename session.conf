{
	instances = {
		ize = Sessionizer("sessioncookie") {
			http->DataReader(f"queries/GetSession.sql") {
				none->DataWriter(f"queries/SetSession.sql") {
					none->Template(f"templates/SessionFail.html");
					single->Return("ized");
				};
				single->Return("ized");
			};
		};

		stop = DataWriter(f"queries/StopSessionLogin.sql") {
			successful->Sessionizer(
				"sessioncookie", sessioncloser = true
			) {
				http->Return("stopped");
			};
		};
	};
}
