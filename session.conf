{
	instances = {
		begin = Sessionizer("session") {
			http_branch = MySqlQuerier(f"queries/GetSession.sql", params = ["session"]) {
				none_branch = MySqlQuerier(f"queries/SetSession.sql", params = ["session"]) {
					none_branch = Template(f"templates/SessionFail.html");
					single_branch = Return("done");
				};
				single_branch = Return("done");
			};
		};
	};
}