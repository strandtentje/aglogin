{
	instances = {
		ize = Sessionizer("sessioncookie") {
			http->DataReader(f"queries/GetSession.sql") {
				none->DataReader(f"queries/SetSession.sql") {
					none->Template(f"templates/SessionFail.html");
					single->Return("ized");
				};
				single->Return("ized");
			};
		};

		stop = DataReader(f"queries/StopSessionLogin.sql") {
			changed_1->Sessionizer(
				"sessioncookie", sessioncloser = true
			) {
				http->Return("stopped");
			};
		};
	};
}
