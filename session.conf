{
	instances = {
		ize = Sessionizer("session") {
			http_branch = MySqlQuerier(
				f"queries/GetSession.sql", 
				params = ["session"], 
				credset = "login"
			) {
				none_branch = MySqlQuerier(
					f"queries/SetSession.sql", 
					params = ["session"], 
					credset = "login"
				) {
					none_branch = Template(f"templates/SessionFail.html");
					single_branch = Return("ized");
				};
				single_branch = Return("ized");
			};
		};
	};
}
