{
	instances = {
		ize = Sessionizer("session") {
			http_branch = DataReader(f"queries/GetSession.sql") {
				none_branch = DataWriter(f"queries/SetSession.sql") {
					none_branch = Template(f"templates/SessionFail.html");
					single_branch = Return("ized");
				};
				single_branch = Return("ized");
			};
		};

		stop = DataWriter(f"queries/StopSessionLogin.sql") {
			successful_branch = Sessionizer(
				"session", sessioncloser = true
			) {
				http_branch = Return("stopped");
			};
		};
	};
}
